// Menu
// --------------------------------------------------
.site-nav-toggle {
  position: static;
  float: right;
}


.menu {
  float: right;
  margin: 8px 0 0 0;

  +mobile() {
    margin: 20px 0 0 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  br {
    display: none;
    +mobile(){
      display: block;
    }
  }

  .menu-item {
    margin: 0;
    +mobile() { display: block; min-width: 15%;}
  }

  .menu-item a {
    padding: 0 10px;
    background: none;
    border: none;
    border-radius: 2px;
    transition-property: background;

    +mobile() {
      text-align: center;
    }

    &:hover { background: #e1e1e1; }
  }

  a::before {
      display: none;

      +mobile() { display: block; }
    }

  +mobile() { float: none; }
}
